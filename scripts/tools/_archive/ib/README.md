# Archived IB Gateway tools

These scripts targeted Interactive Brokers Gateway and legacy `capital_provider` / `order_manager` modules.
They are kept for historical reference only. Active trading uses Bybit (`bybit_worker`, `bybit_order_manager`).
